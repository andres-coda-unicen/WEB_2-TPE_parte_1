<?php $pageTitle = 'Productos'; ?>
<?php require __DIR__ . '/../componentes/header.phtml'; ?>

<section class="page-hero">
  <h1>Todos los Productos</h1>
  <p>Explorá nuestra selección completa</p>
</section>

<!-- Filtro por categoría -->
<div class="filter-bar">
  <a href="<?= BASE_URL ?>/producto" class="filter-chip <?= !isset($_GET['cat']) ? 'active' : '' ?>">
    Todo
  </a>
  <?php foreach ($categorias as $cat): ?>
    <a href="<?= BASE_URL ?>/categorias/producto?id=<?= $cat['id_categoria'] ?>" class="filter-chip">
      <?= htmlspecialchars($cat['nombre']) ?>
    </a>
  <?php endforeach; ?>
</div>

<?php if (empty($productos)): ?>
  <div class="empty-state">
    <p>No hay productos cargados todavía.</p>
  </div>
<?php else: ?>
  <div class="products-grid">
    <?php foreach ($producto as $p): ?>
      <?php
        require __DIR__ . '/componentes/cardProducto.phtml';
      ?>
    <?php endforeach; ?>
  </div>
<?php endif; ?>

<?php require __DIR__ . '/../componentes/footer.phtml'; ?>