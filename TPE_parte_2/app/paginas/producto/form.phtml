<?php
$editando = isset($producto);
$pageTitle = $editando ? 'Editar Producto' : 'Nuevo Producto';
?>
<?php require __DIR__ . '/../componentes/header.phtml'; ?>

<div class="form-page">
  <div class="form-page-header">
    <a href="<?= BASE_URL ?>/admin/producto" class="btn btn-ghost">← Volver</a>
    <h1><?= $editando ? 'Editar Producto' : 'Nuevo Producto' ?></h1>
  </div>

  <form method="POST"
    action="<?= BASE_URL . ($editando ? '/admin/producto/actualizar' : '/admin/producto/guardar') ?>"
    class="form-card">

    <?php if ($editando): ?>
      <input type="hidden" name="id_producto" value="<?= $producto['id_producto'] ?>">
    <?php endif; ?>

    <div class="form-row">
      <div class="form-group">
        <label for="nombre">Nombre del producto *</label>
        <input type="text" id="nombre" name="nombre" required value="<?= htmlspecialchars($producto['nombre'] ?? '') ?>"
          placeholder="Ej: Auriculares Bluetooth">
      </div>

      <div class="form-group">
        <label for="id_categoria">Categoría *</label>
        <select id="id_categoria" name="id_categoria" required>
          <option value="">— Seleccioná una categoría —</option>
          <?php foreach ($categorias as $cat): ?>
            <option value="<?= $cat['id_categoria'] ?>" <?= isset($producto) && $producto['id_categoria'] == $cat['id_categoria'] ? 'selected' : '' ?>>
              <?= htmlspecialchars($cat['nombre']) ?>
            </option>
          <?php endforeach; ?>
        </select>
      </div>
    </div>

    <div class="form-group">
      <label for="descripcion">Descripción</label>
      <textarea id="descripcion" name="descripcion" rows="4"
        placeholder="Describí el producto..."><?= htmlspecialchars($producto['descripcion'] ?? '') ?></textarea>
    </div>

    <div class="form-row">
      <div class="form-group">
        <label for="precio">Precio (ARS) *</label>
        <input type="number" id="precio" name="precio" step="0.01" min="0" required
          value="<?= $producto['precio'] ?? '' ?>" placeholder="0.00">
      </div>

      <div class="form-group">
        <label for="stock">Stock disponible *</label>
        <input type="number" id="stock" name="stock" min="0" required value="<?= $producto['stock'] ?? '0' ?>"
          placeholder="0">
      </div>
    </div>

    <div class="form-group">
      <label for="imagen_url">URL de imagen</label>
      <input type="url" id="imagen_url" name="imagen_url" value="<?= htmlspecialchars($producto['imagen_url'] ?? '') ?>"
        placeholder="https://ejemplo.com/imagen.jpg">
      <span class="form-hint">Ingresá la URL de la imagen del producto.</span>
    </div>

    <!-- Preview de imagen -->
    <?php if (!empty($producto['imagen_url'])): ?>
      <div class="form-preview">
        <img src="<?= htmlspecialchars($producto['imagen_url']) ?>" alt="Preview">
      </div>
    <?php endif; ?>

    <div class="form-actions">
      <a href="<?= BASE_URL ?>/admin/producto" class="btn btn-secondary">Cancelar</a>
      <button type="submit" class="btn btn-primary">
        <?= $editando ? 'Guardar cambios' : 'Crear producto' ?>
      </button>
    </div>
  </form>
</div>

<?php require __DIR__ . '/../componentes/footer.phtml'; ?>