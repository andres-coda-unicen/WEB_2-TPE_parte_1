<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><?= htmlspecialchars($pageTitle ?? 'TiendaOnline') ?></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=DM+Sans:wght@300;400;500&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="<?= BASE_URL ?>/public/css/style.css">
</head>

<body>

  <header class="conteiner-header">
    <div class="header-inner">
      <a href="<?= BASE_URL ?>/" class="logo">
        <span class="logo-icon">◆</span>
        <span>Tienda<strong>Online</strong></span>
      </a>
      <nav class="main-nav">
        <a href="<?= BASE_URL ?>/productos">Productos</a>
        <a href="<?= BASE_URL ?>/categorias">Categorías</a>
        <?php if (Auth::estaLogueado()): ?>
          <span class="nav-divider">|</span>
          <a href="<?= BASE_URL ?>/admin/productos" class="nav-admin">Admin</a>
          <a href="<?= BASE_URL ?>/logout" class="nav-logout">Salir</a>
        <?php else: ?>
          <a href="<?= BASE_URL ?>/login" class="nav-login">Admin</a>
        <?php endif; ?>
      </nav>
    </div>
  </header>

  <main class="site-main">
    <div class="container">

      <?php if (!empty($_SESSION['exito'])): ?>
        <div class="alert alert-ok"><?= htmlspecialchars($_SESSION['exito']) ?></div>
        <?php unset($_SESSION['exito']); ?>
      <?php endif; ?>

      <?php if (!empty($_SESSION['error'])): ?>
        <div class="alert alert-error"><?= htmlspecialchars($_SESSION['error']) ?></div>
        <?php unset($_SESSION['error']); ?>
      <?php endif; ?>